/*
Login to https://coverflex.com/meal and paste this on the console
Copy the output (value of `result`) and paste it on the token.json file
*/
const result = {
	token: localStorage.getItem('cvrflx_flightdeck_token'),
	refresh: localStorage.getItem('cvrflx_flightdeck_refresh_token'),
};
// eslint-disable-next-line no-unused-expressions
result;
